export default defineAppConfig({
  ui: {
    colors: {
      primary: 'homflo',
      neutral: 'stone'
    },

    button: {
      defaultVariants: {
        color: 'primary',
        variant: 'solid',
        size: 'lg'
      }
    },

    input: {
      defaultVariants: {
        size: 'lg'
      }
    }
  }
})
